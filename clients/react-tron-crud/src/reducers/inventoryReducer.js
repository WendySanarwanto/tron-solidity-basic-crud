const inventoryReducer = (state={}, action) => {
    switch(action.type) {
        // TODO: Add action type handler(s) here
        default: 
            return state;
    }
}

export default inventoryReducer;
